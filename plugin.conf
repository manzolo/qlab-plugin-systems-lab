{
  "name": "systems-lab",
  "description": "Linux Systems: boot, kernel, disks and recovery on a REAL VM, where the boot IS the test — a fault is seeded, the machine is power-cycled, and the serial console is the oracle that says whether it came back",
  "version": "0.2"
}
